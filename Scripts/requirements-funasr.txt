# funasr lazily imports AutoModel, so a missing dependency here does not fail
# until the first transcription attempt. torchaudio in particular is imported at
# module scope by funasr.utils.load_utils — without it the sidecar dies on
# startup while the venv still looks correctly installed.
funasr
torch
torchaudio
soundfile
websockets

# English streaming. FunASR's only streaming model is Chinese-only, so English
# partials come from Nemotron's MLX transducer instead. Optional at runtime: if
# these are missing the sidecar logs it and English falls back to
# sentence-at-a-time, so keep them last.
nemotron-asr-mlx
mlx
