# Identical to requirements-funasr.txt on purpose -- see the comment there.
# Both engine rawValues now install the same all-MLX sidecar dependencies.
mlx
mlx-audio @ git+https://github.com/Blaizzy/mlx-audio.git
numpy
soundfile
websockets
